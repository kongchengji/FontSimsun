day4


函数：


值传递：

int main（）
{
	int a=5；
	fun（a）；//传递变量名即值传递 
	
	//fun（&a）；//传递变量名的地址即地址传递
	
	return 0；
}

//int fun（int *b）//地址传递的接收方式，定义同类型的地址变量，也就是指针来接收。（直接操作实参a所在空间的数据，所以会影响实参的数据）
int  fun（int b）//值传递的接收方式，定义同类型的变量来接收（拷贝数据）（将实参a变量的数据拷贝一份来使用，所有不会影响实参的原数据）
{
	
	retturn 0； //return 结果；
}

练习：
分别通过值传递和地址传递的方式实现 两个数的交换，并且在主函数内检验结果，对比思考两者的区别

主函数 头文件   调用函数  



linux 里面一切设备皆文件


文件io：
文件描述符：
open（）

read（）

write（）

close（）


结合文件io 实现对某个文件的拷贝；
对图片的操作


权限的计算

三类用户权限：
文件所有者    文件所属组  其他用户    r  readd(读权限)  w write（写权限）   x extecue （执行权限）
rwx   		   rwx 		rwx     （具有该项权限则为1，反之为0 -）


111  111 111   =>   0 7 7 7

0664  =>    110 110 100
			rw- rw- r--



练习1：

	1、（read）先创建一个文件1.txt，内容编辑为hi，boy，然后编程实现先用open打开该文件，再将文件的内容读取到新定义的buf【128】里面，并且打印以下内容。
	
	2、（write）编程用open打开一个文件 save.txt ,若文件不存在，先创建，再已可写方式打开，然后将 i am boy 写入该文件内，并且不要有多余的字符


实现文件的了拷贝


首先  将文件1 的内容读取出来放到数组buf里面，
然后再将存放在 buf里面的内容写入文件2里面。



